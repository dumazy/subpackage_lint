name: subpackage_lint
description: A set of linting rules for clean code structure with subpackages.
version: 2.0.0
repository: https://github.com/dumazy/subpackage_lint

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  analyzer: ^7.5.4
  analyzer_plugin: ^0.13.4
  glob: ^2.1.3
  path: ^1.9.0
  yaml: ^3.1.2

dev_dependencies:
  lints: ^6.0.0

