name: subpackage_lint_test
description: tests for subpackage_lint package.
publish_to: "none"
version: 2.0.0

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  subpackage_lint:
    path: ../subpackage_lint

dev_dependencies:
  path: ^1.9.0
  test: ^1.25.0
  

