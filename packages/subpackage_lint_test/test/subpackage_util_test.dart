import 'package:test/test.dart';

void main() {
  group('SubpackageUtil', () {
    // test('get subpackage info for a valid path', () {
    //   final path = 'lib/src/subpackage/foo.dart';
    //   final result = getSubpackageInfo(path);
    //   expect(result, isNotNull);
    //   expect(result!.name, 'subpackage');
    //   expect(result.path, 'lib/src/subpackage');
    // });
  });
}
